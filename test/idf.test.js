const idf = require('../src/idf');

describe("Testing idf file", () => {
    test("", () => {
        expect(idf()).toBe();
    });

    test("", () => {
        expect(idf()).toBe();
    });

    test("", () => {
        expect(idf()).toBe();
    });

});