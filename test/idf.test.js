const idf = require('../src/idf');

describe("", () => {
    test("", () => {
        expect(idf()).toBe();
    });

    test("", () => {
        expect(idf()).toBe();
    });

    test("", () => {
        expect(idf()).toBe();
    });

});