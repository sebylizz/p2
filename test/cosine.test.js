const cosine = require('../src/cosine');

describe("Testing cosine file", () => {
    test("", () => {
        expect(cosine()).toBe();
    });

    test("", () => {
        expect(cosine()).toBe();
    });

    test("", () => {
        expect(cosine()).toBe();
    });

});