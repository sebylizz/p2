const cosine = require('../src/cosine');

describe("", () => {
    test("", () => {
        expect(cosine()).toBe();
    });

    test("", () => {
        expect(cosine()).toBe();
    });

    test("", () => {
        expect(cosine()).toBe();
    });

});